# pcbdev
PCB development


t2dutils: 

First time: 
1. git config --global url."https://github.com/".insteadOf "git@github.com:"
2. git submodule update --init --recursive

All subsequent times (to fetch the latest changes)

git pull --recurse-submodules
